# Sample images

Place your construction images here (e.g. floor plan with walls to remove, bathroom marked for demo), then run:

```bash
node index.js ./samples/your-image.jpg
```

Supported formats: JPG, PNG, GIF, WebP.
