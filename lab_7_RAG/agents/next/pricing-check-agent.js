/**
 * Future: Pricing-check agent – takes a list of work items (from extraction agent
 * or user input) and validates/checks pricing against the RAG pricing document.
 * Can flag missing items or suggest alternatives.
 *
 * Usage (when implemented):
 *   const { retriever } = await buildPricingRAG();
 *   const report = await runPricingCheckAgent({ items: [...], retriever });
 */

// Placeholder – implement when you add the pricing-check agent
export async function runPricingCheckAgent({ items, retriever }) {
  throw new Error("Pricing-check agent not implemented yet. Use construction-pricing-agent for full flow.");
}
