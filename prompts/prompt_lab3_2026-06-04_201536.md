# lab3

Saved: 2026-06-04_201536

---

@lab_3 
Create a HTML + JS + CSS in the same single page. 


the page will support a UI to lunch the agent and call the storyTeller api. 

the UI will include a form for the only following properties: mood, subject, linescount, 
observe the API and the test folder to create a good service from the client 

make sure the client will show loader when the data is loaded. 

at the end present the story in nice UI under the form, with the additional info ( metadata of the result from the api)

make the UI similar to Mateiral UI Components  

to the api add a CORS support - so any page will be able to access the server 
